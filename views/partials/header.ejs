<!doctype html>
<html lang="id">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title><%= title %> | SPK Sarastya</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
<div class="shell">
  <aside class="sidebar">
    <div class="sidebar-brand"><img class="brand-logo" src="/sarastya-logo.png" alt="Logo Sarastya Agility Innovations"><div><strong>Sarastya Agility Innovations</strong><span>Sistem Pendukung Keputusan Rekrutmen PKL/Magang</span></div></div>
    <div class="nav-label">MENU UTAMA</div>
    <% const links = [['dashboard','Dashboard Rekrutmen'],['periods','Periode Seleksi'],['users','Kelola Akun Tim Rekrutmen'],['candidates','Data Kandidat'],['criteria','Kriteria'],['swara','Bobot Kriteria'],['ranking','Rangking Penilaian']]; %>
    <% for (const [route,label] of links) { %>
      <% if (!(user.role === 'recruiter' && ['periods','users','swara'].includes(route))) { %>
        <a class="nav-link <%= page === route ? 'active' : '' %>" href="/<%= route %>"><%= label %></a>
      <% } %>
    <% } %>
    <div class="nav-label account-label">AKUN</div>
    <button class="nav-link nav-logout" type="button" data-open-logout>Keluar</button>
  </aside>
  <main class="main">
    <% const profileName = user.name || (user.role === 'super_admin' ? 'HG Sarastya' : 'Tim Rekrutmen'); %>
    <% const profileLabel = user.role === 'super_admin' ? profileName : `${profileName} - ${user.business_unit}`; %>
    <% const profileInitials = profileName.split(/\s+/).filter(Boolean).slice(0, 2).map(part => part[0]).join('').toUpperCase(); %>
    <header class="topbar">
      <strong><%= title %></strong>
      <details class="profile-menu">
        <summary>
          <span class="profile-avatar"><%= profileInitials %></span>
          <span class="profile-summary-text">
            <strong><%= profileLabel %></strong>
            <small><%= user.username %></small>
          </span>
        </summary>
        <div class="profile-dropdown">
          <div class="profile-info">
            <span>Profil Pengguna</span>
            <strong><%= profileLabel %></strong>
            <small><%= user.role === 'super_admin' ? 'Super Admin' : user.business_unit %></small>
          </div>
          <div class="profile-actions">
            <a class="btn profile-edit" href="/profile">Edit Profil</a>
            <button class="btn danger profile-logout" type="button" data-open-logout>Keluar</button>
          </div>
        </div>
      </details>
    </header>
    <div class="modal-backdrop" id="logout-modal" hidden>
      <div class="modal-card" role="dialog" aria-modal="true" aria-labelledby="logout-title">
        <div class="modal-icon">!</div>
        <h2 id="logout-title">Keluar dari Sistem?</h2>
        <p>Apakah Anda yakin ingin mengakhiri sesi dan keluar dari sistem?</p>
        <div class="modal-actions">
          <button class="btn secondary" type="button" data-close-logout>Batal</button>
          <a class="btn danger" href="/logout">Keluar</a>
        </div>
      </div>
    </div>
    <section class="content">
      <% if (flash) { %>
        <div class="toast <%= flash.type === 'error' ? 'error' : '' %>" role="status" aria-live="polite">
          <%= flash.message %>
        </div>
      <% } %>
